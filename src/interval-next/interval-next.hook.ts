import { useCallback } from "react"

// TODO: Like useInterval but your callback can be async (promise) and it will only wait again if the promise resolves.

// TODO: Potentially, also implement an option with a `next` callback.
